# 📱 Clipboard Bridge - Android 应用

完整功能的 Android 客户端，用于与 Clipboard Bridge 桌面服务同步剪贴板。

## ✨ 功能特性

- 🔄 **双向同步** - 从电脑获取或发送到电脑
- 🤖 **智能同步** - 自动判断同步方向
- 💾 **自动保存配置** - 服务器地址和 Token 持久化
- 🎨 **Material Design 3** - 现代化 UI 设计
- 🌓 **深色模式** - 自动适配系统主题
- 📱 **Android 7.0+** - 支持 API 24 及以上

## 🏗️ 技术栈

- **Kotlin** - 100% Kotlin 代码
- **Jetpack Compose** - 声明式 UI
- **Material Design 3** - 最新设计规范
- **Kotlin Coroutines** - 异步处理
- **DataStore** - 配置持久化
- **OkHttp** - HTTP 网络请求
- **ViewModel** - MVVM 架构

## 📦 构建和安装

### 方式 1: 使用 Android Studio

1. 克隆仓库：
```bash
git clone https://github.com/copypasteengine/clipboard-bridge.git
cd clipboard-bridge/android-app
```

2. 用 Android Studio 打开 `android-app` 目录

3. 等待 Gradle 同步完成

4. 连接 Android 设备或启动模拟器

5. 点击 Run 按钮（或 Shift+F10）

### 方式 2: 命令行构建

```bash
cd android-app

# Debug 版本
./gradlew assembleDebug

# Release 版本（需要配置签名）
./gradlew assembleRelease

# APK 位置
# app/build/outputs/apk/debug/app-debug.apk
# app/build/outputs/apk/release/app-release.apk
```

### 安装到设备

```bash
# 通过 adb 安装
adb install app/build/outputs/apk/debug/app-debug.apk
```

## 🎯 使用说明

### 首次配置

1. 打开应用
2. 点击右上角设置图标 ⚙️
3. 输入电脑的 IP 地址和端口，例如：`http://192.168.1.100:5678`
4. 如果设置了 Token，输入 Token
5. 点击"保存"

### 功能说明

**从电脑获取：**
- 点击"从电脑获取"按钮
- 电脑剪贴板内容会复制到手机
- 显示"✓ 已从电脑同步"

**发送到电脑：**
- 在手机上复制内容
- 点击"发送到电脑"按钮
- 内容会发送到电脑剪贴板
- 显示"✓ 已发送到电脑"

**智能同步：**
- 自动判断同步方向
- 如果手机为空，从电脑获取
- 如果电脑为空，发送到电脑
- 如果内容相同，提示"已同步"

**测试连接：**
- 点击连接状态卡片右侧的刷新按钮
- 测试与服务器的连接状态

## 📱 界面预览

```
┌────────────────────────────┐
│  Clipboard Bridge      ⚙️  │
├────────────────────────────┤
│  ● 已连接                  │
│  http://192.168.1.100:5678│
├────────────────────────────┤
│  📱 手机剪贴板         🔄  │
│  Hello from phone          │
├────────────────────────────┤
│  💻 电脑剪贴板         🔄  │
│  Hello from PC             │
│  更新时间: 2 分钟前        │
├────────────────────────────┤
│  [ ⬇️ 从电脑获取 ]         │
│  [ ⬆️ 发送到电脑 ]         │
│  [ 🔄 智能同步 ]           │
└────────────────────────────┘
```

## 🔧 故障排查

### 无法连接服务器

检查：
- [ ] 手机和电脑在同一 WiFi
- [ ] 服务器地址格式正确（包含 `http://`）
- [ ] 电脑防火墙允许端口
- [ ] 桌面程序正在运行

### Token 验证失败

确保：
- Token 与电脑 `config.json` 中一致
- Token 区分大小写
- 如果未设置 Token，留空即可

### 构建错误

尝试：
```bash
# 清理构建
./gradlew clean

# 重新构建
./gradlew assembleDebug
```

## 📂 项目结构

```
android-app/
├── app/
│   ├── src/main/
│   │   ├── java/com/copypasteengine/clipboardbridge/
│   │   │   ├── ClipboardBridgeApp.kt        # Application 类
│   │   │   ├── MainActivity.kt               # 主 Activity
│   │   │   ├── data/
│   │   │   │   └── ClipboardRepository.kt   # 数据层
│   │   │   ├── network/
│   │   │   │   └── ClipboardApiService.kt   # API 服务
│   │   │   └── ui/
│   │   │       ├── ClipboardBridgeScreen.kt # 主界面
│   │   │       ├── ClipboardViewModel.kt     # ViewModel
│   │   │       ├── SettingsDialog.kt         # 设置对话框
│   │   │       └── theme/                    # 主题配置
│   │   ├── res/                              # 资源文件
│   │   └── AndroidManifest.xml               # 清单文件
│   └── build.gradle.kts                      # 应用构建配置
├── build.gradle.kts                          # 项目构建配置
├── settings.gradle.kts                       # Gradle 设置
└── gradle.properties                         # Gradle 属性
```

## 🚀 发布

### 生成签名密钥

```bash
keytool -genkey -v -keystore my-release-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias clipboard-bridge
```

### 配置签名

在 `app/build.gradle.kts` 中添加：

```kotlin
android {
    signingConfigs {
        create("release") {
            storeFile = file("my-release-key.jks")
            storePassword = "your-password"
            keyAlias = "clipboard-bridge"
            keyPassword = "your-password"
        }
    }
    
    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("release")
            // ...
        }
    }
}
```

### 构建 Release APK

```bash
./gradlew assembleRelease
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**与桌面服务配合使用，享受无缝剪贴板同步！** 🎉

